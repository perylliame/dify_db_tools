identity:
  name: dify_db_tools
  author: perylliame
  label:
    en_US: dify_db_tools
    zh_Hans: dify_db_tools
    pt_BR: dify_db_tools
description:
  human:
    en_US: A database tool that follows the dify plugin specification
    zh_Hans: A database tool that follows the dify plugin specification
    pt_BR: A database tool that follows the dify plugin specification
  llm: A database tool that follows the dify plugin specification
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: Query string
      zh_Hans: 查询语句
      pt_BR: Query string
    human_description:
      en_US: A database tool that follows the dify plugin specification
      zh_Hans: A database tool that follows the dify plugin specification
      pt_BR: A database tool that follows the dify plugin specification
    llm_description: A database tool that follows the dify plugin specification
    form: llm
extra:
  python:
    source: tools/dify_db_tools.py
